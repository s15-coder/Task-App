import 'package:http/http.dart';

class AuthService {
  Future login(
    String email,
    String password,
  ) async {}
  Future register(
    String email,
    String password,
    String name,
  ) async {}
}
